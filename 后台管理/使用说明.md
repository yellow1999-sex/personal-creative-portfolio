# 作品集网站后台管理软件

双击 `启动后台管理软件.cmd`。浏览器会自动打开后台；如果没有自动打开，请查看同目录的 `后台地址.txt`。

在后台中点击网页内容即可编辑。上传图片、视频、音乐会写入网站的 `public` 文件夹；保存后会保留，公开网站也会读取相同结果。

“备份网站”会在网站根目录生成可恢复备份；“检查网站”会执行构建；GitHub 与 Vercel 只会在你主动填写并点击发布后才会使用。
