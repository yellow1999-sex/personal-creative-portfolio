# 网站可视化管理器使用说明

双击 `启动网站管理器.cmd`，管理器会自动在浏览器中打开。

## 第一次使用

1. 打开右上角“发布中心”。
2. 点击“登录 GitHub”，在 GitHub 官方窗口完成一次登录。
3. 填写自己的 GitHub 仓库地址，发布分支通常保持 `main`。
4. 点击“连接 Vercel”，在 Vercel 官方页面使用自己的账号登录，并导入这个 GitHub 仓库一次。
5. 把部署完成后的 Vercel 网站地址填回发布中心，点击“保存连接”。

完成以上一次性设置后，后续更新只需要点击“发布上线”，管理器会自动检查网站、提交 GitHub并触发 Vercel部署，不再重复登录。

管理器不会保存 GitHub 密码、访问令牌或 Vercel 密钥。开源后的每位使用者都必须使用自己的账号重新登录。

## 修改网站

- 点击中间预览中的文字、图片、视频或模块。
- 在右侧修改内容、尺寸、颜色或显示状态。
- 上传的图片会自动纠正方向、限制超大尺寸并转换为 WebP；图片内容不会被强制裁切。
- 图片窗口默认使用 16:9；人像精修默认使用 3:4。选中图片后可以改成 16:9、21:9、2.35:1、4:3、1:1、3:4、2:3或自定义比例。
- 点击“保存当前修改”。
- 首页背景视频和 BGM 可以在左侧直接快速替换。
- 空白的人像精修图片窗口可以直接点击后上传图片。

## 发布流程

1. 点击“备份网站”。
2. 点击“检查网站”。
3. 检查通过后点击“发布上线”。
4. 管理器推送到 GitHub；Vercel 绑定该仓库后会自动部署。
